var searchYouTube = (options, callback) => {
  // TODO
  $.ajax({
    url: 'url goes here',
    success: function(data) {
      // update the state
      
    },
    error: function(data) {
      // tell the user
    }
  });
};

window.searchYouTube = searchYouTube;
